# 개발 로그 — 2026-03-13

---

## 1. 웹 기반 글로벌 패스 맵 메이커 구현

### 배경

기존 글로벌 패스 맵(`global_map.json`)은 Gazebo 시뮬레이터 환경의 이전 좌표계를 기반으로 제작되어
현재 GLB 3D 모델과 위치가 맞지 않는 문제가 있었다.  
ROS2 `global_map_maker.py`를 이용한 재생성은 X server 없이 WSL에서 실행이 어렵고,
시뮬레이터 가동 중 작업해야 하는 번거로움이 있었다.

→ 프론트엔드에서 직접 GLB 모델 위를 클릭하여 맵을 그릴 수 있는 **웹 기반 맵 메이커**를 구현.

### 신규 파일: `FRONTEND_WEB/src/views/MapMakerView.vue`

Three.js 기반 탑뷰(Orthographic Camera) 맵 편집기.

**주요 기능:**

| 기능 | 설명 |
|------|------|
| 탑뷰 고정 카메라 | OrthographicCamera, 회전 없이 패닝/줌만 허용 |
| 수동 모드 | 좌클릭으로 노드 1개씩 추가 |
| 자동 보간 모드 | 꺾임점만 찍으면 지정 간격(기본 1.0m)으로 노드 자동 생성 |
| 마커 시각화 | 주황 큰 점(꺾임점) + 초록 작은 점(보간 노드) + 주황 선(경로) |
| 좌표 미리보기 | 마우스 올리면 UTM 좌표 실시간 표시 |
| 실행 취소 | 마지막 꺾임점 + 해당 구간 보간 노드 전체 제거 |
| JSON 복사 | `global_map.json` 형식(nodes + links)으로 클립보드 복사 |

**좌표 변환:**
```js
// scene → UTM
utmX = sceneX + UTM_ORIGIN_X  // 304411.645
utmY = -sceneZ + UTM_ORIGIN_Y  // 3892836.76

// UTM → scene
sceneX = utmX - UTM_ORIGIN_X
sceneZ = -(utmY - UTM_ORIGIN_Y)
```

**자동 보간 로직:**
```js
// 두 꺾임점 사이를 interpDist 간격으로 선형 분할
const steps = Math.floor(distance / interpDist)
for (let i = 0; i <= steps; i++) {
  const t = i / steps
  interpolated.push(lerp(p1, p2, t))
}
```

**클릭 미스매치 수정:**
- `containerRef.getBoundingClientRect()` → `renderer.domElement.getBoundingClientRect()` 로 변경
- 컨테이너와 캔버스 DOM 위치 차이로 인한 raycasting 오차 제거

**조명 설정 (탑뷰에서 벽 윤곽 인식을 위해):**
- 정수리 메인광: `DirectionalLight(white, 2.5)` + PCFSoft 그림자(2048px)
- 측면 보조광 3방향: 청색 / 주황 / 연두

### 라우터 및 네비게이션 추가

- `router/index.js` — `/mapmaker` 경로 추가 (`MapMakerView`, `requiresAuth: true`)
- `App.vue` — 상단 네비게이션에 **맵 메이커** 탭 추가

---

## 2. 글로벌 패스 맵 갱신

맵 메이커를 통해 현재 GLB 3D 모델에 맞춰 새 경로 데이터를 직접 제작.

| 항목 | 이전 | 이후 |
|------|------|------|
| 노드 수 | 44개 | 172개 |
| 링크 수 | 43개 | 171개 |
| 노드 간격 | 불균일 (0.3~3.2m) | 균일 (~0.4m) |
| GLB 정합 | 맞지 않음 | 맞춤 |

갱신 절차:
```bash
# 파일 업데이트
BACKEND/BACKEND_WAS/app/domain/map/global_map.json

# MongoDB 재적재
docker exec mongo mongosh robocop --eval "db.map.drop()"
docker cp global_map.json mongo:/tmp/global_map.json
docker exec mongo mongoimport --db robocop --collection map --file /tmp/global_map.json
```

---

## 3. 제어 탭 — 글로벌 패스 맵 추가

`RobotControlView.vue` 레이아웃 변경:

```
이전: [ 카메라 + 라이다 ]  [ 조작패드 ]
이후: [ 카메라 + 라이다 ]  [ 글로벌 패스 맵 (w-72) ]  [ 조작패드 ]
```

- `MapViewer3D` 컴포넌트를 중앙 패널에 추가
- 로봇 운전 중 실시간 위치 확인 가능

---

## 4. MapViewer3D 조명 개선

기존 조명이 너무 어두워 3D 모델 식별이 어렵던 문제 개선.

```js
// 이전
AmbientLight(white, 0.6) + DirectionalLight(white, 0.8)

// 이후
AmbientLight(white, 1.8)
+ DirectionalLight(white, 2.0)  // 수직 메인광
+ DirectionalLight(#cce8ff, 1.0)  // 전면 청색 보조광
+ DirectionalLight(#ffd0aa, 0.6)  // 후면 주황 보조광
```

배경색: `#1a1a2e` (진남색) → `#2c3e50` (회청색)

---

## 5. 프로젝트 회고록 작성

`RETROSPECTIVE.md` 신규 생성.

주요 내용:
- FastAPI vs Django 선택 근거
- roslibpy → pure asyncio WebSocket 전환 과정 및 이유
- Python GIL, 직렬화/역직렬화, CPU 사용률 측정 원리
- 언어/프레임워크별 ROS 연동 비교 (Python / Node.js / Java / C++)

---

## 6. 맵 메이커 — 서버 저장 기능 추가

### 배경

맵 메이커에서 JSON을 복사한 후 수동으로 MongoDB에 재적재하던 불편함 제거.  
버튼 하나로 현재 그린 맵을 백엔드에 바로 반영.

### 백엔드

- `map_service.py` — `update_points(data: PointsData)` 메서드 추가
  - 기존 `map` 컬렉션 `drop()` 후 새 데이터 `insert_one()`
- `map_controller.py` — `PUT /api/map` 엔드포인트 추가
  - 성공 시 `{ ok: true, nodes: N, links: N }` 반환
  - 실패 시 HTTP 500

### 프론트엔드 (`MapMakerView.vue`)

- **"🚀 서버에 저장"** 버튼 추가 (노드 0개 또는 저장 중이면 비활성)
- 저장 결과 메시지 색상 분기:
  - 성공 → 보라 (`✅ 서버 저장 완료 — 노드 N개, 링크 N개`)
  - 실패 → 빨강 (`❌ 저장 실패: ...`)

---

## 7. 제어 탭 — 글로벌 패스 맵 로봇 표시 및 follow-cam

### 문제

- 제어 탭의 `MapViewer3D`에 로봇 마커가 표시되지 않던 문제
- 경로 노드/링크도 표시되지 않던 문제 (props 미전달)
- 정지 버튼이 수동 모드가 아닐 때도 활성화되어 있던 문제

### 수정 내용

**`MapViewer3D.vue`**
- `robotsStore.displayRobots` watch 추가 → SSE로 갱신된 위치를 자동으로 로봇 마커에 반영
- `followSeq` prop 추가 (Number | null): 해당 로봇 위치로 카메라 타깃이 부드럽게 이동 (LERP 0.05)
- props로 맵 데이터가 없을 때 `/api/map`을 내부에서 직접 fetch하여 경로 노드/링크 표시

```js
// animate 루프 내 follow-cam
if (props.followSeq !== null) {
  const pos = robotPositions.value[props.followSeq]
  if (pos && pos.x !== 0 && pos.y !== 0) {
    const dx = sc.x - controls.target.x
    const dz = sc.z - controls.target.z
    controls.target.x += dx * 0.05
    controls.target.z += dz * 0.05
    camera.position.x += dx * 0.05
    camera.position.z += dz * 0.05
  }
}
```

**`RobotControlView.vue`**
- `<MapViewer3D :followSeq="selectedSeq" />` — 선택된 로봇으로 follow-cam 활성화
- 정지 버튼: `isManualMode`가 false이면 다른 방향 버튼과 동일하게 비활성 처리

> **참고**: 로봇 진행 방향(yaw) 기반 맵 회전은 현재 미구현.  
> `/robot_{seq}/status` 백엔드 파싱에 yaw 필드를 추가하면 구현 가능.

---

## 8. 환경 정보

- 작업일: 2026-03-13
- OS: Windows 11 (Win32 10.0.26200)
- Frontend: Vue 3 + Vite + Three.js
- Backend: Python 3.10 + FastAPI
- DB: MongoDB (Docker)
- 시뮬레이터: Gazebo + TurtleBot3
