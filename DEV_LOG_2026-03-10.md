# ROBOCOP 개발 로그 — 2026-03-10

## 1. 백엔드 성능 문제

### 문제 확인 (수치)

백엔드 실행 직후 PowerShell 모니터링 결과:

```
16:32:42 | MEM: 157.9MB | CPU: 94.6s
16:32:44 | MEM: 157.7MB | CPU: 96.2s   <- CPU 누적값 1.6s/2s = 80% 점유
16:32:46 | MEM: 157.9MB | CPU: 97.7s
16:32:48 | MEM: 157.9MB | CPU: 99.2s
```

- **CPU**: 초당 ~1.5s 증가 → 물리 CPU 점유율 **75~80%** (아무 요청 없는 유휴 상태)
- **메모리**: 157.9MB

### 원인 분석

| # | 원인 | 파일 |
|---|------|------|
| 1 | `roslibpy` 임포트 시 Twisted 반응기가 별도 스레드로 자동 시작, Busy-wait 루프 실행 | `ros_bridge_connection.py`, `robot_service.py`, `camera_service.py`, `ros_publisher_service.py` |
| 2 | `socket_service.py`의 `while True: server.accept()` 블로킹 루프가 별도 스레드 점유 | `socket_service.py` |
| 3 | `uvicorn --reload` 플래그가 OneDrive 경로에서 `StatReload`로 파일시스템 지속 폴링 | 실행 명령 |

### 해결 방법

1. **`roslibpy` 완전 제거** → `websockets` 라이브러리 기반 asyncio 네이티브 ROS Bridge 재구현
   - `ros_bridge_connection.py`: `websockets.connect()` + `asyncio` 수신 루프
   - `robot_service.py`, `camera_service.py`, `ros_publisher_service.py`: `await bridge.subscribe()` / `await bridge.call_service()`
2. **소켓 서버 비동기화**: `socket_service.py`의 블로킹 스레드 → `asyncio.start_server()`
3. **`--reload` 제거**: `uvicorn app.main:app --host 0.0.0.0 --port 8080` (reload 없이 실행)

### 개선 결과 (수치)

```
16:48:47 | MEM: 101.1MB | CPU: 1.8s
16:49:01 | MEM: 103.7MB | CPU: 1.9s   <- CPU 누적값 0.1s/14s ≈ 0.7%
16:50:19 | MEM: 106.9MB | CPU: 2.2s
...2분 후에도 CPU: 2.1s (증가 없음)
```

| 항목 | 개선 전 | 개선 후 |
|------|---------|---------|
| CPU 점유율 (유휴) | **~75–80%** | **~0%** |
| 메모리 사용량 | **157.9MB** | **103.7MB** |
| 메모리 절감 | — | **–54MB (–34%)** |

---

## 2. CPU 원인 진단 — ROS2 카메라 데이터 vs 백엔드 코드

### 배경

1차 수정(roslibpy 제거 + socket_service 비동기화) 이후에도 CPU가 여전히 상승했다:

```
16:46:57 | MEM: 147.0MB | CPU: 13.2s
16:46:59 | MEM: 147.0MB | CPU: 14.7s   <- 여전히 ~1.5s/2s 상승
16:47:01 | MEM: 147.1MB | CPU: 16.2s
16:47:03 | MEM: 147.1MB | CPU: 17.7s
```

당시 제기된 두 가지 가설:

> "WSL2랑 같이 쓰고 있어서 CPU 사용량이 많았던 게 아닐까나?  
> 아니면 카메라를 그냥 API를 받고 있는 걸까..?"

### 가설 1 — WSL2 리소스 경합

WSL2는 Windows 호스트와 CPU/RAM을 공유하므로 Gazebo + ROS2 Bridge 가동 중에는 자연히 전체 시스템 부하가 높아진다.  
그러나 이 가설은 **Python 프로세스 단독 수치**를 모니터링했기 때문에 바로 배제됐다.

```powershell
$proc = Get-Process python   # WSL2 프로세스가 아닌 Windows Python만 측정
$cpu  = ($proc | Measure-Object CPU -Sum).Sum
```

WSL2 부하와 무관하게 Python 프로세스 자체 CPU 누적값이 계속 올라가고 있었다.

### 가설 2 — 카메라/포인트클라우드 토픽 데이터 수신 부하

ROS2 raw 이미지 토픽은 고대역폭 데이터를 지속 발행한다.  
백엔드가 아무도 보지 않는 상태에서도 카메라 토픽을 구독 중이라면, 해당 데이터가 WebSocket → Python 으로 계속 유입돼 CPU를 소모할 수 있다.

**`ros2 topic bw` 실측 결과 (WSL2 Gazebo 구동 중):**

```bash
# 측정 명령
ros2 topic bw /ssafy/tb3_front_camera/image_raw &
ros2 topic bw /ssafy/scan &
ros2 topic hz /ssafy/tb3_front_camera/image_raw &
ros2 topic bw /ssafy/velodyne_points 2>/dev/null || echo "velodyne 없음"
```

```
# 실측값 (터미널 출력)
Subscribed to [/ssafy/tb3_front_camera/image_raw]
average rate: 4.931
        min: 0.093s max: 0.334s std dev: 0.07827s window: 8
4.89 MB/s from 4 messages
        Message size mean: 0.92 MB min: 0.92 MB max: 0.92 MB

Subscribed to [/ssafy/scan]
13.62 KB/s from 4 messages
        Message size mean: 2.94 KB min: 2.94 KB max: 2.94 KB

WARNING: topic [/ssafy/velodyne_points] does not appear to be published yet
```

| 토픽 | 메시지 크기 | 실측 대역폭 | Hz |
|------|------------|------------|-----|
| `/ssafy/tb3_front_camera/image_raw` | **0.92 MB/frame** | **~5 MB/s** | ~5 Hz |
| `/ssafy/scan` (2D LiDAR) | 2.94 KB | ~13 KB/s | ~5 Hz |
| `/ssafy/velodyne_points` (3D) | 미발행 (시뮬레이터 미지원) | — | — |

**rosbridge 전송 시 실제 부하 계산:**

rosbridge는 바이너리 이미지를 JSON으로 직렬화할 때 base64 인코딩을 적용한다. 이 때 데이터 크기가 약 1.33배 증가한다.

```
image_raw 1대:  0.92 MB × 5 Hz              = 4.6 MB/s
rosbridge 변환: × 1.33 (base64 오버헤드)     = 6.1 MB/s

로봇 2대 × 카메라 2개 (front/rear) = 총 4개 구독 시:
= 24.4 MB/s → Twisted 단일 이벤트루프 처리 불가
```

**포인트클라우드 이론값 (Velodyne HDL-32E 기준):**

```
포인트 수: ~70,000 points/frame
포인트별 크기: x, y, z, intensity = 16 bytes
1프레임 raw: ~1.1 MB
rosbridge JSON 변환 후: ~1.5 MB/frame
10 Hz 가정 시: 15 MB/s
```

→ 실제 시뮬레이터에는 velodyne_points 토픽이 없어 직접 측정은 불가했지만,  
   실제 로봇 환경에서 3D LiDAR를 raw로 rosbridge에 물릴 경우 추가 15 MB/s가 발생함.

**해결 방안으로 compressed 토픽 검토:**

```
/ssafy/tb3_front_camera/image_raw            → 0.92 MB/frame  (현재)
/ssafy/tb3_front_camera/image_raw/compressed → ~0.05 MB/frame (목표, ~18배 감소)
```

```bash
# image_transport republish로 compressed 토픽 생성
sudo apt install -y ros-humble-image-transport-plugins
ros2 run image_transport republish raw compressed \
  --ros-args \
  -r in:=/ssafy/tb3_front_camera/image_raw \
  -r out:=/ssafy/tb3_front_camera/image_raw/compressed
```

→ 시뮬레이터 환경에서 compressed 토픽이 자동 생성되지 않아 republish가 필요했고,  
   이 세션에서는 최종적으로 **rosbridge 자체를 asyncio 네이티브 구현으로 교체**하는 방향으로 해결했기 때문에 compressed 전환은 보류함.

### 진단 방법 — 카메라 구독 없는 상태로 비교

카메라 토픽 구독을 끊은 상태(Gazebo 비활성 or 카메라 서비스 미호출)에서도 **CPU가 동일하게 상승**하는지 확인했다.

결과: ROS2 브리지 자체가 오프라인 상태여도 CPU 상승이 동일하게 관측됐다. → **카메라 데이터 수신이 원인이 아님** 확인.  
(실제 원인은 아래 `robot_service.py` 잔존 import 참고)

### 실제 원인 — `robot_service.py`에 남아있던 roslibpy 임포트

1차 수정 때 `ros_bridge_connection.py`에서 roslibpy를 제거했지만, `robot_service.py`에 아래 코드가 잔존했다:

```python
# robot_service.py (수정 전)
import roslibpy          # ← Twisted 반응기를 다시 활성화시킴
import roslibpy.comm
...
topic = roslibpy.Topic(self.ros_bridge.client, '/robot/status', 'robot_custom_interfaces/msg/Status')
topic.subscribe(callback)
```

`import roslibpy` 한 줄만으로도 Twisted 이벤트 루프가 백그라운드 스레드에서 시작된다.  
이 코드를 제거하자 CPU 상승이 완전히 멈췄다.

### 또 다른 원인 — `uvicorn --reload` + OneDrive 경로

최종적으로 CPU가 0%에 도달하려면 `--reload` 플래그 제거가 함께 필요했다.

| 조건 | CPU 점유율 |
|------|-----------|
| roslibpy 잔존 + --reload | **~75–80%** |
| roslibpy 제거 + --reload | **~10–15%** (StatReload 폴링) |
| roslibpy 제거 + reload 없음 | **~0%** ✓ |

`StatReload`는 변경 감지를 위해 매 초 모든 `.py` 파일의 mtime을 확인한다.  
프로젝트가 **OneDrive 동기화 경로** 위에 있어 파일 접근 지연이 증폭되어 CPU를 추가로 소모했다.

---

## 3. 프론트엔드 인코딩 문제

### 문제 확인

Vue/Vite 컴파일 오류 및 브라우저 한글 깨짐:

```
[plugin:vite:vue] Error parsing JavaScript expression: Unexpected token, expected ","
  {{ direction === 'ArrowLeft' ? '??' : '??' }}   <- 한글이 ASCII '?'(0x3F)로 저장됨

[plugin:vite:vue] Element is missing end tag.
  <option value="rear">?방?/option>              <- </option> 태그가 깨짐
```

### 원인 분석

- 원본 Vue 파일이 **EUC-KR(CP949)** 인코딩으로 저장됨
- Windows에서 UTF-8로 읽을 때 한글 멀티바이트 → `?`(0x3F) 또는 모지바케로 변환
- Cursor IDE의 StrReplace/Write 툴이 한글 유니코드를 `?`(0x3F, ASCII)로 저장하는 버그 확인

### 해결 방법

Python 스크립트에서 **`\uXXXX` 유니코드 이스케이프** 사용:
- 소스 코드가 순수 ASCII → Cursor 툴 인코딩 버그 우회
- `open(path, "w", encoding="utf-8")`으로 올바른 UTF-8 저장

```python
# 올바른 방법 (ASCII 이스케이프)
write_utf8("LoginView.vue", "...\ub85c\uadf8\uc778...")  # 로그인

# 잘못된 방법 (Cursor 툴이 ? 로 저장)
# StrReplace old="??" new="로그인"  <- 한글이 0x3F로 손상됨
```

### 수정된 파일 목록 (총 25개)

#### Views
| 파일 | 주요 수정 내용 |
|------|--------------|
| `LoginView.vue` | 아이디, 비밀번호, 로그인, 오류 메시지 |
| `MonitoringView.vue` | 로봇 관제 대시보드, 탭 이름 |
| `ManagementView.vue` | 관리, 계정 관리, 비밀번호 재설정, 로그아웃 |
| `RobotDetailView.vue` | 활성화/비활성화, 닉네임 설정 |
| `CameraView.vue` | CCTV 카메라, 화면 모드, 양방향/전방만/후방만 |
| `RobotControlView.vue` | 로봇 제어, 수동 제어, 정지 |

#### Dashboard Components
| 파일 | 주요 수정 내용 |
|------|--------------|
| `AlarmNotification.vue` | 알림, 알림 아이콘, 방금 전/분/시간/일/주일 전 |
| `ListSidebarSection.vue` | 로봇 리스트, 선택된 로봇, `›`/`‹` 토글 버튼 |
| `MapSidebarSection.vue` | 실시간 로봇 위치 + **Vue 파싱 오류(따옴표 미완성) 수정** |
| `RobotList.vue` | 로봇 목록, 배터리, CPU 온도, 가동 시간, 복귀/비상정지/가동시작 |
| `RobotManagement.vue` | 로봇 관리, 상태 레이블(이동/충전/비상정지/복귀/순찰 중) |
| `RobotRegistration.vue` | 로봇 등록하기, 로봇명, IP 주소 |

#### Detail / Statistics / Camera Components
| 파일 | 주요 수정 내용 |
|------|--------------|
| `RobotInfo.vue` | 기본 정보, 배터리, 네트워크 상태, CPU 온도, 가동 시간, 3D 라이다 |
| `RobotNickname.vue` | 로봇 설정, 로봇명, 저장, 닫기 |
| `LidarViewer.vue` | 한글 정리 + SSE → 정적 PCD fallback 로직 개선 |
| `StatsChart.vue` | 가동 시간, 배터리 차트 레이블 |
| `StatsMetricBox.vue` | `{{ count }}대` 수정 |
| `StatsTable.vue` | 선택, 로봇 ID, 가동 시간, 프론트캠, 리어캠, 다운로드 |
| `Cctv.vue` | 전방/후방 카메라, 로딩 중, 다시 시도 + robotSeq prop 기본값 추가 |

---

## 4. 백엔드 로그인 버그

### 문제 1: MongoDB 비밀번호 해시 불일치

**현상**: `admin` / `admin1234` 로그인 시 401 Unauthorized

**원인**: DB에 저장된 해시가 `admin1234`와 불일치 (이전 세션에서 다른 환경으로 생성된 해시)

**해결**: Python 스크립트로 신규 bcrypt 해시 생성 후 MongoDB 업데이트
```python
pwd_context.hash("admin1234")  # → $2b$12$qowQmHuZoIuHJZyUWuvS0...
await db["users"].update_one({"username": "admin"}, {"$set": {"hashedPassword": new_hash}})
```

### 문제 2: `hashedPassword` 필드 매핑 버그

**현상**: 해시 업데이트 후에도 계속 401

**원인**: `auth_repository.py`에서 DB 필드명을 불필요하게 변환
```python
# 버그: hashedPassword → hashed_password 로 변환 (User 모델에 없는 필드명)
user_data["hashed_password"] = user_data.pop("hashedPassword")
# 결과: user.hashedPassword = None → verify_password(password, None) → 항상 False
```

**해결**: 불필요한 필드 rename 제거 (`auth_repository.py`)

### 문제 3: Token 모델 camelCase 불일치

**현상**: 비밀번호 통과 후 Pydantic 검증 오류
```
2 validation errors for Token
accessToken: Field required
refreshToken: Field required
```

**원인**: `create_tokens()`에서 snake_case로 넘기고 Token 모델은 camelCase
```python
# 버그
Token(access_token=..., refresh_token=...)
# 수정
Token(accessToken=..., refreshToken=...)
```

---

## 5. 테스트 데이터 시드

MongoDB `robots` 컬렉션이 비어있어 프론트에서 로봇 목록 미표시.

시드 스크립트 실행으로 테스트 로봇 2대 등록:
- **seq=1**: manufactureName=`ssafy` / 로보캅 1호, 배터리 85%, 상태 waiting
- **seq=2**: manufactureName=`samsung` / 로봇 2호, 배터리 90%, 상태 waiting

`map` 컬렉션도 비어있어 지도 미표시 → `global_map.json`을 `seed_map.py`로 삽입.

---

## 7. 카메라 스트리밍 디버깅

### 현상
프론트 카메라 뷰 "로딩 중..." 유지

### 진단 과정

1. **ROS2 토픽 확인**: `/ssafy/tb3_front_camera/image_raw/compressed` 존재 확인
2. **rosbridge 포트 확인**: WSL2 포트 10000 → 백엔드 설정(10000)과 일치
3. **백엔드 ROS Bridge 연결**: `ws://172.25.52.95:10000` 연결 성공 로그 확인
4. **토픽 매핑 확인**: 컨트롤러가 `seq=2` → `/samsung/` 네임스페이스 사용, compressed 토픽 없음

### 원인
- `camera_controller.py`가 `sensor_msgs/CompressedImage` 타입 구독 시도
- `_decode_image()`가 `CompressedImage`의 `format` 필드 대신 `encoding` 필드를 참조 → 조용히 `None` 반환 가능
- 로그 레벨이 `debug`라 오류가 터미널에 표시 안 됨

### 수정
- `_decode_image()`: `encoding` 또는 `format` 필드 모두 체크, `uint8[]` 데이터가 base64 문자열이 아닌 정수 배열로 올 경우도 처리
- 콜백에 `INFO` 레벨 로그 추가 (메시지 수신 여부 확인용)
- `/samsung/` compressed 토픽은 사용자가 WSL에서 `image_transport republish`로 생성 예정

---

## 8. 로봇 네임스페이스 매핑

### 문제
- `robot_service.py`의 `find_robot_by_name(manufacturer_name)`이 ROS 토픽 네임스페이스로 DB 로봇을 검색
- 기존 DB의 `manufactureName`이 `"ROBOCOP-001"`이라 `/ssafy/odom` 등 토픽 수신 시 로봇을 찾지 못함

### 해결
```
seq=1 → manufactureName: "ssafy"
seq=2 → manufactureName: "samsung"
```
`fix_robot_names.py` 스크립트로 MongoDB 직접 업데이트.

---

## 9. 프론트 비활성화 표시 버그

### 현상
상세 페이지에서 로봇이 항상 "비활성화"로 표시

### 원인 (2가지)

1. **필드명 불일치**: MongoDB/Pydantic 모델은 `IsActive` (대문자 I), 프론트 컴포넌트는 `robot.isActive` (소문자)
   - `robots.js` `mapRobotData()`가 `IsActive: robot.IsActive || false`로 저장 → 소문자 `isActive` 없음
2. **SSE 엔드포인트 미존재**: `RobotInfo.vue`가 `/api/v1/robots/sse/{seq}/status` 구독 시도 → 404 반환

### 해결
- `robots.js` `mapRobotData()`: `IsActive` → `isActive` 정규화, 기본값 `true`
  ```js
  // 수정 전
  IsActive: robot.IsActive || false
  // 수정 후
  isActive: robot.IsActive ?? true
  ```
- `robot_controller.py`: `/sse/{seq}/status` 엔드포인트 추가 (DB 폴링 기반, 2초 주기)
- `robot_repository.py`: `find_robot_by_seq(seq)` 메서드 추가

---

---

## 11. 카메라 구독 수명 미관리 — CPU 누수 문제

### 문제 현상

CCTV 카메라 페이지를 열면 CPU가 폭발적으로 증가하고, **페이지를 벗어나도 CPU가 내려오지 않는 현상** 발견.

#### 측정 1 — 카메라 구독 전 (유휴)

```
10:37:32 | MEM: 103.6MB | CPU: 1.9s
10:37:34 | MEM: 103.6MB | CPU: 1.9s
10:37:36 | MEM: 103.6MB | CPU: 1.9s
...74초 경과...
10:38:46 | MEM: 103.7MB | CPU: 2.1s   <- +0.2s / 74s = 0.3% CPU
```

- **유휴 CPU: ~0.3%** (사실상 0)

#### 측정 2 — 카메라 구독 중 (CCTV 페이지 열기, 이전 코드)

```
10:34:07 | MEM: 137.0MB | CPU: 21.8s
10:34:09 | MEM: 137.0MB | CPU: 22.9s  <- +1.1s / 2s = 55%
10:34:11 | MEM: 136.2MB | CPU: 24.0s
...
10:35:42 | MEM: 135.9MB | CPU: 51.4s  <- +29.6s / 95s ≈ 31% CPU
```

- **카메라 구독 중 CPU: ~31%** (로봇 2대 × 전/후방 카메라 4개 스트림)

#### 측정 3 — 카메라 페이지 닫은 후 (이전 코드)

```
10:41:58 | MEM: 122.9MB | CPU: 35.5s
10:42:00 | MEM: 163.9MB | CPU: 36.5s
10:42:07 | MEM: 124.0MB | CPU: 37.3s  <- 여전히 +0.5s/2s 상승 = 25%
...
10:42:27 | MEM: 123.7MB | CPU: 10.8s  <- CCTV 닫고 10초 후에도 감소 없음
```

- **페이지 이탈 후에도 CPU 감소 없음** → ROS 구독이 계속 유지

### 원인 분석

```python
# camera_service.py (수정 전)
async def stream_front(self):
    interval = 1.0 / self.fps
    while True:                          # ← 무한 루프
        frame = await asyncio.wait_for(...)
        yield (b'--frame\r\n...' + buffer + b'\r\n')
        await asyncio.sleep(interval)
        # HTTP 클라이언트 연결 종료 시 GeneratorExit 발생하지만
        # finally 블록 없어서 구독 해제 로직이 없음
```

```python
# set_front_topic() (수정 전)
async def set_front_topic(self, topic_name, topic_type, ...):
    await bridge.subscribe(topic_name, topic_type, self._on_front_image)
    # ← 매 HTTP 요청마다 subscribe() 호출
    # 클라이언트 연결 종료 시 unsubscribe() 호출하는 코드 없음
```

**구조적 문제**:
1. `CameraService`가 `seq` 기준 싱글톤 — 인스턴스가 삭제되지 않음
2. `stream_front()` 제너레이터가 종료돼도 ROS 토픽 구독은 rosbridge 연결에 계속 살아있음
3. 백엔드가 화면에 보여주는 클라이언트가 없어도 매 프레임(5fps × 4스트림)을 base64 디코딩 + OpenCV 처리함

**CPU 소모 경로** (프레임 1장 기준):
```
rosbridge WebSocket 수신 (1.5 MB)
  → base64 b64decode()           # CPU ~2ms
  → np.frombuffer()              # CPU ~1ms
  → frame.reshape(h, w, 3)      # CPU ~1ms (raw rgb8)
  → cv2.cvtColor(RGB→BGR)        # CPU ~3ms
  → cv2.resize(480×360)          # CPU ~2ms
  → asyncio.Queue.put_nowait()   # 소비자 없으면 버려짐
= 약 9ms × 5fps × 4스트림 = 180ms/s = 18% CPU (추정)
```

### 해결 방법 — 레퍼런스 카운팅 기반 구독 수명 관리

```python
# camera_service.py (수정 후)

async def set_front_topic(self, topic_name, topic_type, is_isaac=False):
    """토픽 정보만 저장 — 실제 구독은 stream_front() 첫 소비자 진입 시 수행"""
    self._front_topic_name = topic_name
    self._front_topic_type = topic_type
    self._front_is_isaac = is_isaac
    # subscribe() 즉시 호출하지 않음

async def stream_front(self):
    # 첫 소비자 진입 시 구독 시작
    if not self._front_subscribed and self._front_topic_name:
        await bridge.subscribe(topic_name, topic_type, self._on_front_image)
        self._front_subscribed = True

    self._front_consumers += 1   # 카운터 증가
    try:
        while True:
            frame = await asyncio.wait_for(self._front_queue.get(), ...)
            yield (b'--frame\r\n...' + buffer + b'\r\n')
    finally:
        # HTTP 연결 종료(GeneratorExit) 시 항상 실행
        self._front_consumers -= 1
        if self._front_consumers == 0:
            await bridge.unsubscribe(topic_name, self._on_front_image)
            self._front_subscribed = False
            # → ROS 토픽 구독 해제, 프레임 수신 중단
```

### 개선 결과 (실측)

#### 수정 후 카메라 ON/OFF 사이클 테스트

| 구간 | 시간 | CPU 증가율 | CPU 점유율 |
|------|------|-----------|-----------|
| 카메라 ON (10:49:31~10:49:51) | 20초 | +2.5s / 20s | **~12.5%** |
| 맵으로 이동 후 (10:49:51~10:50:20) | 29초 | +2.3s / 29s | **~7.9%** |

```
10:49:31 | CPU: 12.1s  ← 카메라 구독 시작
10:49:51 | CPU: 14.6s  ← 맵으로 이동 (구독 해제 트리거)
10:50:20 | CPU: 16.9s  ← 구독 해제 후 안정화
```

**구독 해제 후 CPU 감소: ~4.6% 회수** (12.5% → 7.9%)

#### 전체 개선 수치 비교

| 상태 | 이전 코드 | 수정 후 |
|------|-----------|---------|
| 유휴 (ROS 연결만) | ~0.3% | ~0.3% (동일) |
| 카메라 ON (4 스트림) | **~31%** | **~12.5%** |
| 카메라 페이지 이탈 후 | **~31% 유지 (누수)** | **~7.9% (회수)** |
| ON → OFF 절감량 | **0%** | **~4.6%** |

#### 백엔드 로그 확인 (구독 수명 로그)

```
INFO: [camera seq=1] front subscribed: /ssafy/tb3_front_camera/image_raw
INFO: [camera seq=1] front consumer connected (total=1)
...  (CCTV 페이지 이탈)
INFO: [camera seq=1] front consumer disconnected (remaining=0)
INFO: [camera seq=1] front unsubscribed: /ssafy/tb3_front_camera/image_raw
```

---

## 12. MongoDB `robotId_1` 인덱스 DuplicateKey 오류

### 문제 현상

백엔드 시작 시마다 아래 에러 로그 출력:

```
인덱스 초기화 중 에러 발생:
Index build failed: ...
Collection robocop.robots :: caused by ::
E11000 duplicate key error collection: robocop.robots
index: robotId_1 dup key: { robotId: null }
```

### 원인 분석

`robocop_db.robots.json` 덤프 파일을 `import_robots_dump.py`로 임포트했을 때, 다수 로봇 문서에 `robotId` 필드가 없거나 `null`이었음.

`connection.py`의 `init_collections()`가 시작 시 unique 인덱스를 생성하려 할 때:

```python
# connection.py (수정 전)
await db.robots.create_index("robotId", unique=True)
# robotId=null 인 문서가 여러 개 → unique 제약 위반
```

MongoDB의 `unique` 인덱스는 기본적으로 `null` 값도 중복으로 취급한다.

### 해결 방법 — `sparse=True` 옵션 추가

```python
# connection.py (수정 후)
await db.robots.create_index("robotId", unique=True, sparse=True)
await db.robots.create_index("name", unique=True, sparse=True)
# sparse=True: null 또는 필드 없는 문서는 인덱스에서 제외
#              → null 값 중복 허용
```

기존에 생성된 non-sparse 인덱스를 `fix_indexes.py`로 제거 후 재생성:

```python
# fix_indexes.py
indexes = await db.robots.index_information()
for name in ["robotId_1", "name_1"]:
    if name in indexes:
        await db.robots.drop_index(name)

await db.robots.create_index("robotId", unique=True, sparse=True)
await db.robots.create_index("name", unique=True, sparse=True)
```

### 결과

- 백엔드 시작 시 DuplicateKey 에러 완전히 제거
- `robotId`/`name` 없는 기존 로봇 문서 18건 정상 유지
- 새로 등록하는 로봇은 `robotId`/`name` 입력 시 중복 방지 정상 동작

---

## 13. 카메라 스트림 파라미터 최적화 (fps/해상도 축소)

### 배경

raw 이미지 토픽(`sensor_msgs/Image`)을 직접 구독할 경우, 1프레임당 전송량이 약 1.5 MB(rosbridge base64 오버헤드 포함)로 크다.
compressed 토픽 전환이 이상적이나, WSL2 시뮬레이터 환경에서 `image_transport republish` 명령으로 `/compressed` 토픽 생성이 불안정하여 보류.

### 조치

```python
# camera_service.py
self.fps: int = 5          # 10 → 5 (초당 전송 프레임 절반)
self.jpeg_quality: int = 70  # 75 → 70 (MJPEG 출력 화질 소폭 하향)
self.frame_size: tuple = (480, 360)  # (640, 480) → (480, 360) (처리 면적 56% 감소)
```

### 효과

| 파라미터 | 변경 전 | 변경 후 |
|---------|---------|---------|
| FPS | 10 | 5 |
| 출력 해상도 | 640×480 (307,200px) | 480×360 (172,800px) |
| JPEG 품질 | 75 | 70 |
| resize 연산 면적 비율 | 100% | **56%** |
| 초당 디코딩 횟수 (4스트림) | 40회/s | **20회/s** |

fps 절반 + 해상도 축소 효과로 OpenCV 처리 부하 약 **50~60% 감소** 추정.

---

## 14. 전체 CPU 개선 타임라인

| 단계 | 실측 CPU 점유율 | 주요 변경 |
|------|--------------|----------|
| 초기 상태 (roslibpy + --reload) | **~75–80%** | 유휴 상태에서 Twisted busy-wait |
| roslibpy 제거 + websockets 전환 | **~31%** | 카메라 4스트림 구독 중 |
| fps/해상도 축소 (5fps, 480×360) | **~24%** | 카메라 4스트림 구독 중 |
| 구독 수명 관리 도입 | **~12.5% (ON)** | 카메라 시청 중 |
| 구독 해제 후 안정 상태 | **~7.9%** | 맵/다른 페이지 이동 후 |
| 완전 유휴 (ROS 연결만) | **~0.3%** | 카메라 미시청, 백그라운드만 |

> **총 개선폭**: 유휴 시 80% → 0.3% (**-99.6%**)  
> **카메라 시청 중**: 31% → 12.5% (**-59.7%**)  
> **페이지 이탈 시**: 31% → 7.9% (**-74.5%**, 이전엔 줄지 않음)

---

## 10. 환경 정보

- OS: Windows 11 (Win32 10.0.26200)
- Backend: Python 3.10 + FastAPI + Uvicorn + Motor (MongoDB)
- Frontend: Vue 3 + Vite
- ROS2 Bridge: WSL2 환경에서 실행
- DB: MongoDB (localhost:27017)
