# ROBOCOP 개발 로그 — 2026-03-12

## 1. 카메라 구독 수명 미관리 — CPU 누수 문제

### 문제 현상

CCTV 카메라 페이지를 열면 CPU가 폭발적으로 증가하고, **페이지를 벗어나도 CPU가 내려오지 않는 현상** 발견.

#### 측정 1 — 카메라 구독 전 (유휴)

```
10:37:32 | MEM: 103.6MB | CPU: 1.9s
10:37:34 | MEM: 103.6MB | CPU: 1.9s
10:37:36 | MEM: 103.6MB | CPU: 1.9s
...74초 경과...
10:38:46 | MEM: 103.7MB | CPU: 2.1s   <- +0.2s / 74s = 0.3% CPU
```

- **유휴 CPU: ~0.3%** (사실상 0)

#### 측정 2 — 카메라 구독 중 (CCTV 페이지 열기, 수정 전 코드)

```
10:34:07 | MEM: 137.0MB | CPU: 21.8s
10:34:09 | MEM: 137.0MB | CPU: 22.9s  <- +1.1s / 2s = 55%
10:34:11 | MEM: 136.2MB | CPU: 24.0s
...
10:35:42 | MEM: 135.9MB | CPU: 51.4s  <- +29.6s / 95s ≈ 31% CPU
```

- **카메라 구독 중 CPU: ~31%** (로봇 2대 × 전/후방 카메라 4개 스트림)

#### 측정 3 — 카메라 페이지 닫은 후 (수정 전 코드)

```
10:41:58 | MEM: 122.9MB | CPU: 35.5s
10:42:00 | MEM: 163.9MB | CPU: 36.5s
10:42:07 | MEM: 124.0MB | CPU: 37.3s  <- 여전히 +0.5s/2s 상승 = 25%
10:42:47 | MEM: 124.5MB | CPU: 43.4s  <- CCTV 닫고도 감소 없음
```

- **페이지 이탈 후에도 CPU 감소 없음** → ROS 구독이 계속 유지

### 원인 분석

```python
# camera_service.py (수정 전)
async def stream_front(self):
    interval = 1.0 / self.fps
    while True:                          # ← 무한 루프
        frame = await asyncio.wait_for(...)
        yield (b'--frame\r\n...' + buffer + b'\r\n')
        await asyncio.sleep(interval)
        # HTTP 클라이언트 연결 종료 시 GeneratorExit 발생하지만
        # finally 블록 없어서 구독 해제 로직이 없음
```

```python
# set_front_topic() (수정 전)
async def set_front_topic(self, topic_name, topic_type, ...):
    await bridge.subscribe(topic_name, topic_type, self._on_front_image)
    # ← 매 HTTP 요청마다 subscribe() 호출
    # 클라이언트 연결 종료 시 unsubscribe() 호출하는 코드 없음
```

**구조적 문제**:
1. `CameraService`가 `seq` 기준 싱글톤 — 인스턴스가 삭제되지 않음
2. `stream_front()` 제너레이터가 종료돼도 ROS 토픽 구독은 rosbridge 연결에 계속 살아있음
3. 백엔드가 화면에 보여주는 클라이언트가 없어도 매 프레임(5fps × 4스트림)을 base64 디코딩 + OpenCV 처리함

**CPU 소모 경로** (프레임 1장 기준):
```
rosbridge WebSocket 수신 (1.5 MB)
  → base64 b64decode()           # CPU ~2ms
  → np.frombuffer()              # CPU ~1ms
  → frame.reshape(h, w, 3)      # CPU ~1ms (raw rgb8)
  → cv2.cvtColor(RGB→BGR)        # CPU ~3ms
  → cv2.resize(480×360)          # CPU ~2ms
  → asyncio.Queue.put_nowait()   # 소비자 없으면 버려짐
= 약 9ms × 5fps × 4스트림 = 180ms/s = 18% CPU (추정)
```

### 해결 방법 — 레퍼런스 카운팅 기반 구독 수명 관리

```python
# camera_service.py (수정 후)

async def set_front_topic(self, topic_name, topic_type, is_isaac=False):
    """토픽 정보만 저장 — 실제 구독은 stream_front() 첫 소비자 진입 시 수행"""
    self._front_topic_name = topic_name
    self._front_topic_type = topic_type
    self._front_is_isaac = is_isaac
    # subscribe() 즉시 호출하지 않음

async def stream_front(self):
    # 첫 소비자 진입 시 구독 시작
    if not self._front_subscribed and self._front_topic_name:
        await bridge.subscribe(topic_name, topic_type, self._on_front_image)
        self._front_subscribed = True

    self._front_consumers += 1   # 카운터 증가
    try:
        while True:
            frame = await asyncio.wait_for(self._front_queue.get(), ...)
            yield (b'--frame\r\n...' + buffer + b'\r\n')
    finally:
        # HTTP 연결 종료(GeneratorExit) 시 항상 실행
        self._front_consumers -= 1
        if self._front_consumers == 0:
            await bridge.unsubscribe(topic_name, self._on_front_image)
            self._front_subscribed = False
            # → ROS 토픽 구독 해제, 프레임 수신 중단
```

### 개선 결과 (실측)

#### 수정 후 카메라 ON/OFF 사이클 테스트

```
10:49:31 | CPU: 12.1s  ← 카메라 구독 시작 (5fps, 480×360)
10:49:51 | CPU: 14.6s  ← 맵으로 이동 (구독 해제 트리거)
10:50:20 | CPU: 16.9s  ← 구독 해제 후 안정화
```

| 구간 | 시간 | CPU 증가율 | CPU 점유율 |
|------|------|-----------|-----------|
| 카메라 ON (10:49:31~10:49:51) | 20초 | +2.5s / 20s | **~12.5%** |
| 맵으로 이동 후 (10:49:51~10:50:20) | 29초 | +2.3s / 29s | **~7.9%** |
| **구독 해제 효과** | — | — | **약 -4~5% CPU 감소** |

**구독 해제 후 CPU 감소: ~4.6% 회수** (12.5% → 7.9%)

#### 전체 개선 수치 비교

| 상태 | 수정 전 | 수정 후 |
|------|---------|---------|
| 유휴 (ROS 연결만) | ~0.3% | ~0.3% (동일) |
| 카메라 ON (4 스트림) | **~31%** | **~12.5%** |
| 카메라 페이지 이탈 후 | **~31% 유지 (누수)** | **~7.9% (회수)** |
| ON → OFF 절감량 | **0%** | **~4.6%** |

#### 백엔드 로그 확인 (구독 수명 로그)

```
INFO: [camera seq=1] front subscribed: /ssafy/tb3_front_camera/image_raw
INFO: [camera seq=1] front consumer connected (total=1)
...  (CCTV 페이지 이탈)
INFO: [camera seq=1] front consumer disconnected (remaining=0)
INFO: [camera seq=1] front unsubscribed: /ssafy/tb3_front_camera/image_raw
```

남은 ~8% 백그라운드 CPU 구성:

| 항목 | 비고 |
|------|------|
| ROS Bridge WebSocket 연결 유지 | 상시 연결 필요, 제거 불가 |
| 로봇 상태 SSE 폴링 (`/sse/{seq}/status`) | 2초 주기 DB 조회 |
| uvicorn 기본 오버헤드 | 요청 처리, 이벤트 루프 |

---

## 2. MongoDB `robotId_1` 인덱스 DuplicateKey 오류

### 문제 현상

백엔드 시작 시마다 아래 에러 로그 출력:

```
인덱스 초기화 중 에러 발생:
Index build failed: ...
Collection robocop.robots :: caused by ::
E11000 duplicate key error collection: robocop.robots
index: robotId_1 dup key: { robotId: null }
```

### 원인 분석

`robocop_db.robots.json` 덤프 파일을 `import_robots_dump.py`로 임포트했을 때, 다수 로봇 문서에 `robotId` 필드가 없거나 `null`이었음.

`connection.py`의 `init_collections()`가 시작 시 unique 인덱스를 생성하려 할 때:

```python
# connection.py (수정 전)
await db.robots.create_index("robotId", unique=True)
# robotId=null 인 문서가 여러 개 → unique 제약 위반
```

MongoDB의 `unique` 인덱스는 기본적으로 `null` 값도 중복으로 취급한다.

### 해결 방법 — `sparse=True` 옵션 추가

```python
# connection.py (수정 후)
await db.robots.create_index("robotId", unique=True, sparse=True)
await db.robots.create_index("name", unique=True, sparse=True)
# sparse=True: null 또는 필드 없는 문서는 인덱스에서 제외
#              → null 값 중복 허용
```

기존에 생성된 non-sparse 인덱스를 `fix_indexes.py`로 제거 후 재생성:

```python
# fix_indexes.py
indexes = await db.robots.index_information()
for name in ["robotId_1", "name_1"]:
    if name in indexes:
        await db.robots.drop_index(name)

await db.robots.create_index("robotId", unique=True, sparse=True)
await db.robots.create_index("name", unique=True, sparse=True)
```

### 결과

- 백엔드 시작 시 DuplicateKey 에러 완전히 제거
- `robotId`/`name` 없는 기존 로봇 문서 18건 정상 유지
- 새로 등록하는 로봇은 `robotId`/`name` 입력 시 중복 방지 정상 동작

---

## 3. 카메라 스트림 파라미터 최적화 (fps/해상도 축소)

### 배경

raw 이미지 토픽(`sensor_msgs/Image`)을 직접 구독할 경우, 1프레임당 전송량이 약 1.5 MB(rosbridge base64 오버헤드 포함)로 크다.
compressed 토픽 전환이 이상적이나, WSL2 시뮬레이터 환경에서 `image_transport republish` 명령으로 `/compressed` 토픽 생성이 불안정하여 보류.

### 조치

```python
# camera_service.py
self.fps: int = 5            # 10 → 5 (초당 전송 프레임 절반)
self.jpeg_quality: int = 70  # 75 → 70 (MJPEG 출력 화질 소폭 하향)
self.frame_size: tuple = (480, 360)  # (640, 480) → (480, 360) (처리 면적 56% 감소)
```

### 효과

| 파라미터 | 변경 전 | 변경 후 |
|---------|---------|---------|
| FPS | 10 | 5 |
| 출력 해상도 | 640×480 (307,200px) | 480×360 (172,800px) |
| JPEG 품질 | 75 | 70 |
| resize 연산 면적 비율 | 100% | **56%** |
| 초당 디코딩 횟수 (4스트림) | 40회/s | **20회/s** |

fps 절반 + 해상도 축소 효과로 OpenCV 처리 부하 약 **50~60% 감소** 추정.

### 실측 검증

파라미터 축소 + 구독 수명 관리 도입 후 ON/OFF 사이클 실측:

```
10:49:31 | CPU: 12.1s  ← 카메라 구독 시작 (5fps, 480×360)
10:49:51 | CPU: 14.6s  ← 맵으로 이동 (구독 해제 트리거)
10:50:20 | CPU: 16.9s  ← 구독 해제 후 안정화
```

| 구간 | 시간 | CPU 증가 | CPU 점유율 |
|------|------|---------|-----------|
| 카메라 ON (10:49:31~10:49:51) | 20초 | +2.5s | **~12.5%** |
| 맵으로 이동 후 (10:49:51~10:50:20) | 29초 | +2.3s | **~7.9%** |
| **구독 해제 효과** | — | — | **약 -4~5% CPU 감소** |

---

## 4. 전체 CPU 개선 타임라인

| 단계 | 날짜 | 실측 CPU 점유율 | 주요 변경 |
|------|------|--------------|----------|
| 초기 상태 (roslibpy + --reload) | 03-10 | **~75–80%** | 유휴 상태에서 Twisted busy-wait |
| roslibpy 제거 + websockets 전환 | 03-10 | **~31%** | 카메라 4스트림 구독 중 |
| fps/해상도 축소 (5fps, 480×360) | 03-10 | **~24%** | 카메라 4스트림 구독 중 |
| 구독 수명 관리 도입 | 03-12 | **~12.5% (ON)** | 카메라 시청 중 |
| 구독 해제 후 안정 상태 | 03-12 | **~7.9%** | 맵/다른 페이지 이동 후 |
| 완전 유휴 (ROS 연결만) | 03-12 | **~0.3%** | 카메라 미시청, 백그라운드만 |

> **총 개선폭**: 유휴 시 80% → 0.3% (**-99.6%**)  
> **카메라 시청 중**: 31% → 12.5% (**-59.7%**)  
> **페이지 이탈 시**: 31% → 7.9% (**-74.5%**, 수정 전엔 감소 없음)

---

## 5. 프론트엔드 — 사이드바 LED 및 맵 로봇 위치 미표시 버그

### 문제 현상

- 사이드바 로봇 목록의 활성 LED가 `ssafy`/`samsung` 로봇에서 항상 꺼진 상태(비활성) 표시
- 실시간 맵(`RobotMap.vue`)에 로봇 위치 마커가 전혀 나타나지 않음

### 원인 분석

**버그 1 — `ListSidebarSection.vue` : 제조사명 정규식 필터**

```js
// 수정 전
const isActive = (robot) =>
  (robot.isActive === true || robot.IsActive === true)
  && /^robot_\d+$/.test(robot.manufactureName);  // ← "ssafy", "samsung" 은 통과 못 함
```

`/^robot_\d+$/` 정규식이 `"ssafy"`, `"samsung"` 문자열을 걸러내 항상 `false` 반환.

**버그 2 — `RobotMap.vue` : `isActive` 대소문자 불일치**

```js
// 수정 전 (setupSSE 내부 activeRobots 필터)
const activeRobots = robotsStore.robots
  .filter(robot => robot?.IsActive === true);  // ← 대문자 I
```

`robotsStore`의 `mapRobotData()`는 `isActive`(소문자 i)로 정규화하여 저장하는데, 필터는 `IsActive`(대문자 I)를 참조 → 항상 빈 배열.

### 해결

```js
// ListSidebarSection.vue (수정 후)
const isActive = (robot) =>
  robot.isActive === true || robot.IsActive === true;
// 정규식 조건 완전 제거

// RobotMap.vue setupSSE (수정 후)
const activeRobots = robotsStore.robots
  .filter(robot =>
    (robot.seq === 1 || robot.seq === 2) &&
    (robot?.isActive === true || robot?.IsActive === true)  // 양쪽 대소문자 모두 처리
  );
```

---

## 6. 프론트엔드 — "관리" 탭 접속 시 페이지 먹통 버그

### 문제 현상

상단 내비게이션의 "관리" 탭을 클릭하면 브라우저가 완전히 응답 불능 상태가 됨. API 요청도 발생하지 않음.

### 원인 분석

`ManagementView.vue`가 `<UserList />` 컴포넌트를 props 없이 렌더링:

```html
<!-- ManagementView.vue (수정 전) -->
<UserList />
<!-- users prop 없이 사용 → UserList 내부에서 users.length 접근 시 TypeError -->
```

`UserList.vue` 내부:
```js
// 수정 전 — props에 default 없음
defineProps({
  users: Array,    // ← undefined 가능
  openModal: Function,
})
// template에서 v-if="users.length === 0" → TypeError: Cannot read properties of undefined
```

추가로 `UserList.vue` 전체 파일에 mojibake(EUC-KR 인코딩 잔재)가 남아있어 Vue 파서가 오류를 발생시킴.

### 해결

```js
// UserList.vue (수정 후)
defineProps({
  users: {
    type: Array,
    default: () => [],   // ← undefined 대신 빈 배열
  },
  openModal: {
    type: Function,
    default: () => {},   // ← undefined 대신 noop
  },
});
```

파일 전체를 UTF-8로 재작성하여 mojibake 완전 제거.

---

## 7. 프론트엔드 — 라이다 404 / 3D PointCloud2 전환

### 문제 1 — `/api/v1/lidar/sse/undefined` 404 에러

**현상**: 라이다 컴포넌트 마운트 시 다음 요청 발생:
```
GET /api/v1/lidar/sse/undefined  →  404 Not Found
```

**원인**:
1. `LidarViewer.vue`가 마운트 시 `props.robotSeq`가 아직 `undefined`인 상태에서 SSE 연결 시도
2. 백엔드에 `/sse/{seq}` 엔드포인트 자체가 누락됨

**해결**:
```js
// LidarViewer.vue — 마운트 가드 추가
watch(() => props.robotSeq, (seq) => {
  if (!seq) return;   // ← undefined/null 일 때 연결하지 않음
  connectSSE(seq);
}, { immediate: true });
```

백엔드에 `GET /api/v1/lidar/sse/{seq}` 엔드포인트 추가.

---

### 문제 2 — 2D LaserScan → 3D PointCloud2 전환

시뮬레이터에서 `/ssafy/velodyne_points`, `/samsung/velodyne_points` 토픽이 발행되는 것을 확인, 3D 라이다 데이터로 전환.

**백엔드 변경** (`lidar_controller.py`):
- `sensor_msgs/LaserScan` 구독 → `sensor_msgs/PointCloud2` 구독
- rosbridge가 `uint8[]`를 base64 문자열로 전송 → `base64.b64decode()` 처리
- `struct.unpack_from('<f', ...)` 으로 x, y, z, intensity 파싱
- 최대 3000포인트 다운샘플링 (성능 제한)

```python
MAX_POINTS = 3000
step = max(1, n_points // MAX_POINTS)  # 다운샘플링 비율
for i in range(0, n_points, step):
    off = i * point_step
    x = struct.unpack_from('<f', data_bytes, off + x_off)[0]
    y = struct.unpack_from('<f', data_bytes, off + y_off)[0]
    z = struct.unpack_from('<f', data_bytes, off + z_off)[0]
```

**프론트엔드 변경** (`LidarViewer.vue`):
- Three.js 카메라 위치를 `(0, -10, 8)`, `up=(0,0,1)` 로 변경 → X-Y 평면 조감 뷰
- `GridHelper` 추가 (X-Y 평면 격자)
- 원점 마커 (빨간 원, `CircleGeometry`) 추가
- 포인트 색상을 **Z축 높이 기반** 그라디언트로 변경:
  - 낮음(z ≈ -0.5m) → 파란색
  - 중간(z ≈ 0.5m) → 녹색
  - 높음(z ≈ 1.5m) → 빨간색

---

## 8. 프론트엔드 — 경로 제어 맵 노드 선택이 초기화되는 버그

### 기능 배경

`RobotDetailView.vue`에 맵 노드 클릭으로 Navigate/Patrol 명령을 보내는 경로 제어 UI를 추가:
- 노드 1개 선택 → "이동" 버튼 활성화
- 노드 2개 이상 → "순찰" 버튼 활성화

### 문제 현상

맵을 열고 노드를 선택하면 수 초 내에 선택이 사라지고 맵이 새로고침됨. 노드를 클릭해도 유지가 안 돼 버튼 사용 불가.

### 원인 분석

`RobotMap.vue`에 두 개의 deep watcher가 있었음:

```js
// 수정 전 — (1) 로봇 목록 deep watch
watch(() => robotsStore.robots, () => {
  // SSE 연결 전부 끊고 재연결
  eventSources.forEach(source => source.close())
  eventSources = setupSSE()
}, { deep: true })
// → 배터리, 상태, 위치 등 어떤 속성 변경에도 SSE 재설정 = selectedNodes 초기화

// 수정 전 — (2) robot prop deep watch
watch(() => props.robot, (newRobot) => {
  selectedNodes.value = []  // ← deep이라 robot.status 변경에도 항상 초기화
  updateChartSeries()
}, { deep: true })
```

로봇 위치가 SSE로 2초마다 업데이트될 때마다 deep watch가 발동 → SSE 재연결 + 노드 선택 초기화.

### 해결 — watcher를 최소 변경 범위로 한정

```js
// 수정 후 — (1) seq 목록이 바뀔 때만 SSE 재설정
watch(
  () => robotsStore.robots.map(r => r.seq).join(','),
  () => {
    eventSources.forEach(source => source.close())
    eventSources.clear()
    eventSources = setupSSE()
  }
)
// "1,2" → "1,2,3" 처럼 로봇 수가 바뀔 때만 반응, 속성 변경 무시

// 수정 후 — (2) robot의 seq가 바뀔 때만 노드 초기화
watch(() => props.robot?.seq, (newSeq, oldSeq) => {
  if (newSeq && newSeq !== oldSeq) {
    selectedNodes.value = []
    updateChartSeries()
  }
})
// 다른 로봇으로 전환했을 때만 초기화, 같은 로봇의 속성 변경은 무시
```

### 결과

- 노드 선택 후 수 초가 지나도 선택 유지됨
- 이동/순찰 버튼이 정상적으로 활성화되어 명령 전송 가능

---

## 9. robot_patrol — 경로 도달 후 추가 명령 무시 버그

### 문제 현상

Navigate/Homing으로 목적지에 도달한 뒤 다시 Navigate/Patrol 명령을 보내면 로봇이 즉시 waiting 상태로 복귀하고 이동을 시작하지 않는 현상.

```
[robot_patrol]: 경로에 도달했습니다. Waiting service 호출하여 대기모드로 전환.
[robot_status_publisher]: [TEMP STOP] Temporarily stopping the robot.
[robot_status_publisher]: 🚨[WAITING] Switching to waiting mode.🚨
← 이후 새 명령을 보내도 수 초 내에 다시 waiting으로 복귀
```

### 원인 분석 — 레이스 컨디션

`robot_patrol.cpp`의 `follow_path()` 에서 경로 도달 처리 시 상태 변수를 초기화하지 않아 발생하는 **레이스 컨디션**이었다.

```
1차 navigate 완료
  → path_queue_  비워짐 (노드 전부 pop)
  → global_path_received_ = true  ← 이전 값 그대로 남음
  → mode → "waiting"

2차 navigate 명령 수신
  → one_robot_status_pub: mode → "navigate"
  → global_path_planner: navigate_plan 비동기 계산 시작
            ↓ (수 ms 안에)
  → pose_callback 즉시 실행
        global_path_received_ = true  (이전 경로 플래그)
        path_queue_.empty()  → true  (이전 경로 소모됨)
            ↓
  → follow_current_path() → 큐 비어있음 → reached=true 즉시 반환
            ↓
  → callTempStop() + callWaitingService() 재호출
            ↓
  → 새 경로가 도착하기도 전에 다시 waiting 복귀
```

핵심: `global_path_received_` 플래그와 `path_queue_` 가 경로 완료 시점에 리셋되지 않아, 다음 명령이 도착했을 때 `follow_current_path()`가 "빈 큐 = 도착"으로 오해함.

### 해결 방법 — 도착 시 상태 초기화

```cpp
// robot_patrol.cpp — follow_path() 내 경로 도달 처리 (수정 후)
bool reached = follow_current_path();
if (reached) {
    RCLCPP_INFO(this->get_logger(),
        "경로에 도달했습니다. Waiting service 호출하여 대기모드로 전환.");
    // 추가: 다음 명령의 새 경로가 올 때까지 대기하도록 플래그·큐 초기화
    global_path_received_ = false;
    clearQueue(path_queue_);
    clearQueue(save_path_queue_);
    callTempStop();
    callWaitingService();
    return;
}
```

`global_path_received_ = false` 로 리셋하면:
1. 새 navigate/patrol 명령 수신
2. `global_path_planner`가 경로 계산 후 토픽 발행
3. `global_path_callback`에서 `global_path_received_ = true` 로 세팅
4. 그 이후 `follow_path()` 에서 경로 추종 시작 → 정상 동작

### 결과

- 1차 navigate 완료 후 2차 명령도 정상 실행됨
- `waiting` → `navigate` → 이동 → `waiting` 사이클이 반복 동작

---

## 11. 프론트엔드 — 로봇 맵(RobotMap) ECharts → Three.js 3D 뷰어 전환

### 배경

기존 `RobotMap.vue`는 ECharts 기반 2D 맵으로 항공 이미지(`row-map.png`)를 배경으로 사용했으나, 두 가지 문제가 있었다.

1. **ECharts `graphic` 요소 관리 충돌**: SSE로 로봇 위치가 업데이트될 때마다 `setOption()`이 재호출되면서 `graphic` 배열이 초기화되어 배경 이미지가 사라지는 현상.
2. **좌표 불일치**: `row-map.png`에 georeferencing 정보가 없어 UTM 좌표계와 시각적으로 맞추는 것이 불가.

→ Three.js 기반 3D 뷰어로 전환하여 Blender로 제작한 GLB 오피스 모델 위에 로봇 위치와 경로 노드를 실좌표로 렌더링.

### 신규 파일 — `MapViewer3D.vue`

**좌표계 변환** (`utmToScene`):

```js
const UTM_ORIGIN_X = 304411.645  // c101.world GPS origin → UTM
const UTM_ORIGIN_Y = 3892836.76  // 실측 로봇 2대 데이터로 교정

function utmToScene(utmX, utmY) {
  return {
    x: utmX - UTM_ORIGIN_X,      // UTM East  → Three.js +X
    z: -(utmY - UTM_ORIGIN_Y),   // UTM North → Three.js -Z (Y-up 반전)
  }
}
```

**UTM_ORIGIN 교정 과정**: Gazebo 시뮬레이터에서 로봇 2대의 실측 좌표를 수집하여 역산.

| 로봇 | Gazebo (x, y) | GPS UTM | 역산 origin Y |
|------|--------------|---------|--------------|
| Robot 1 | (-23.13, -4.79) | (304388.52, 3892831.97) | 3892836.76 |
| Robot 2 | (3.20, -6.62) | (304414.85, 3892830.14) | 3892836.76 |

초기 `c101.world` 파싱 기반 origin(3892842.473)보다 약 **5.7m 남쪽**이 실제 GPS 기준점임을 확인.

**GLB 모델 로드 (`/public/models/office.glb`)**:

```js
loader.load('/models/office.glb',
  (gltf) => {
    // Blender 내부 원점과 Gazebo 원점 오프셋 보정
    gltf.scene.position.set(2.4, 0, 1.5)
    scene.add(gltf.scene)
    // GLB 메쉬 목록 수집 (디버그 클릭용)
    gltf.scene.traverse(obj => { if (obj.isMesh) glbMeshes.push(obj) })
  },
  undefined,
  () => buildFallbackMap()  // GLB 없으면 map-floor.png 텍스처 바닥 fallback
)
```

GLB position offset (2.4, 0, 1.5) 결정 근거:
- X +2.4m: Blender 원점이 Gazebo 원점보다 서쪽에 설정되어 있어 동쪽으로 보정
- Z +1.5m: 남쪽 보정 (최대 2.2m 이내로 제한 — 초과 시 북쪽 경로 노드가 건물 밖으로 나감)

**렌더링 요소**:
- 경로 노드: `SphereGeometry(0.25)`, 선택 시 핫핑크(`0xff4081`)
- 경로 링크: `LineBasicMaterial` 반투명 파란선
- 로봇 마커: `ConeGeometry(0.5, 1.5)` + `PointLight` 후광

**외부 API**:
- `updatePosition(seq, utmX, utmY)` — SSE에서 호출
- `clearPosition(seq)` — SSE 오류 시 호출
- `resetNodeSelection()` — 명령 전송 후 호출

### 디버그 좌표 도구

좌표계 정렬 검증을 위해 디버그 모드 추가:
- **좌표계 ON 버튼** (좌측 하단): `THREE.AxesHelper(15)` + 좌표 정보 패널 표시
- **휠클릭**: GLB 메쉬 표면 → Y=0 바닥 평면 순으로 raycasting, 클릭 지점의 scene XYZ + UTM 좌표 표시

```js
// 휠클릭 (button=1) → 디버그 좌표 표시
// 좌클릭 (button=0) → 경로 노드 선택 (기존 동작)
```

### `RobotMap.vue` 변경 사항

- `vue-echarts` import 및 `<v-chart>` 제거, `<MapViewer3D>` 컴포넌트로 교체
- SSE 콜백: `map3dRef.value.updatePosition(seq, x, y)` 호출로 변경
- 중복 선언된 `setupSSE()` 함수 제거 (구버전 잔류 코드 정리)

---

## 12. 마무리 정리 — 배포 준비

### 가동 시간 표시 제거

실시간 `startAt` 값이 DB 기준으로 고정되어 오류 값(예: 9291시간)을 표시하는 문제로 인해 가동 시간 UI 항목 제거.

- `RobotList.vue` — 가동 시간 `<div>` 블록 제거
- `RobotInfo.vue` — 가동 시간 `<p>` 항목 제거

### 비활성 로봇 DB 정리

배포 전 더미 데이터 제거. `IsActive: false` 로봇 16개 삭제.

```js
db.robots.deleteMany({ IsActive: false })  // Deleted: 16
```

잔류 로봇:
- `seq: 1` — 2F-221 (활성)
- `seq: 2` — 2F-222 (활성)

### 로그인 화면 데모 계정 안내 추가

`LoginView.vue` 로그인 폼 하단에 데모 계정 정보 박스 추가.

```
아이디: admin
비밀번호: admin1234
```

### 라우터 가드 인코딩 오류 수정

`router/index.js`의 `beforeEach` 가드에 인코딩 깨진 한글 `alert()` 두 개 제거.
인증 실패 / 이미 로그인 시 경고창 없이 조용히 리다이렉트 하도록 변경.

```js
// 이전: alert('ë¡ê·¸?¸ì´ ?ì?©ë??');  → 깨진 문자열
// 이후: alert 제거, next('/login') 또는 next('/') 로만 처리
```

### ngrok 외부 공유 설정

개발 서버를 외부에 임시 공유하기 위해 `vite.config.js`에 `allowedHosts: true` 추가.

```js
server: {
  allowedHosts: true,  // ngrok 등 외부 도메인 허용
  ...
}
```

---

## 13. 환경 정보

- OS: Windows 11 (Win32 10.0.26200)
- Backend: Python 3.10 + FastAPI + Uvicorn + Motor (MongoDB)
- Frontend: Vue 3 + Vite
- ROS2 Bridge: WSL2 (Ubuntu 22.04, ROS2 Humble)
- DB: MongoDB (localhost:27017)
- 시뮬레이터: Gazebo + TurtleBot3 (ssafy/samsung 네임스페이스, 로봇 2대)
