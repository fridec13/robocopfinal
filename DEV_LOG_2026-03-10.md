# ROBOCOP 개발 로그 — 2026-03-10

## 1. 백엔드 성능 문제

### 문제 확인 (수치)

백엔드 실행 직후 PowerShell 모니터링 결과:

```
16:32:42 | MEM: 157.9MB | CPU: 94.6s
16:32:44 | MEM: 157.7MB | CPU: 96.2s   <- CPU 누적값 1.6s/2s = 80% 점유
16:32:46 | MEM: 157.9MB | CPU: 97.7s
16:32:48 | MEM: 157.9MB | CPU: 99.2s
```

- **CPU**: 초당 ~1.5s 증가 → 물리 CPU 점유율 **75~80%** (아무 요청 없는 유휴 상태)
- **메모리**: 157.9MB

### 원인 분석

| # | 원인 | 파일 |
|---|------|------|
| 1 | `roslibpy` 임포트 시 Twisted 반응기가 별도 스레드로 자동 시작, Busy-wait 루프 실행 | `ros_bridge_connection.py`, `robot_service.py`, `camera_service.py`, `ros_publisher_service.py` |
| 2 | `socket_service.py`의 `while True: server.accept()` 블로킹 루프가 별도 스레드 점유 | `socket_service.py` |
| 3 | `uvicorn --reload` 플래그가 OneDrive 경로에서 `StatReload`로 파일시스템 지속 폴링 | 실행 명령 |

### 해결 방법

1. **`roslibpy` 완전 제거** → `websockets` 라이브러리 기반 asyncio 네이티브 ROS Bridge 재구현
   - `ros_bridge_connection.py`: `websockets.connect()` + `asyncio` 수신 루프
   - `robot_service.py`, `camera_service.py`, `ros_publisher_service.py`: `await bridge.subscribe()` / `await bridge.call_service()`
2. **소켓 서버 비동기화**: `socket_service.py`의 블로킹 스레드 → `asyncio.start_server()`
3. **`--reload` 제거**: `uvicorn app.main:app --host 0.0.0.0 --port 8080` (reload 없이 실행)

### 개선 결과 (수치)

```
16:48:47 | MEM: 101.1MB | CPU: 1.8s
16:49:01 | MEM: 103.7MB | CPU: 1.9s   <- CPU 누적값 0.1s/14s ≈ 0.7%
16:50:19 | MEM: 106.9MB | CPU: 2.2s
...2분 후에도 CPU: 2.1s (증가 없음)
```

| 항목 | 개선 전 | 개선 후 |
|------|---------|---------|
| CPU 점유율 (유휴) | **~75–80%** | **~0%** |
| 메모리 사용량 | **157.9MB** | **103.7MB** |
| 메모리 절감 | — | **–54MB (–34%)** |

---

## 2. 프론트엔드 인코딩 문제

### 문제 확인

Vue/Vite 컴파일 오류 및 브라우저 한글 깨짐:

```
[plugin:vite:vue] Error parsing JavaScript expression: Unexpected token, expected ","
  {{ direction === 'ArrowLeft' ? '??' : '??' }}   <- 한글이 ASCII '?'(0x3F)로 저장됨

[plugin:vite:vue] Element is missing end tag.
  <option value="rear">?방?/option>              <- </option> 태그가 깨짐
```

### 원인 분석

- 원본 Vue 파일이 **EUC-KR(CP949)** 인코딩으로 저장됨
- Windows에서 UTF-8로 읽을 때 한글 멀티바이트 → `?`(0x3F) 또는 모지바케로 변환
- Cursor IDE의 StrReplace/Write 툴이 한글 유니코드를 `?`(0x3F, ASCII)로 저장하는 버그 확인

### 해결 방법

Python 스크립트에서 **`\uXXXX` 유니코드 이스케이프** 사용:
- 소스 코드가 순수 ASCII → Cursor 툴 인코딩 버그 우회
- `open(path, "w", encoding="utf-8")`으로 올바른 UTF-8 저장

```python
# 올바른 방법 (ASCII 이스케이프)
write_utf8("LoginView.vue", "...\ub85c\uadf8\uc778...")  # 로그인

# 잘못된 방법 (Cursor 툴이 ? 로 저장)
# StrReplace old="??" new="로그인"  <- 한글이 0x3F로 손상됨
```

### 수정된 파일 목록 (총 25개)

#### Views
| 파일 | 주요 수정 내용 |
|------|--------------|
| `LoginView.vue` | 아이디, 비밀번호, 로그인, 오류 메시지 |
| `MonitoringView.vue` | 로봇 관제 대시보드, 탭 이름 |
| `ManagementView.vue` | 관리, 계정 관리, 비밀번호 재설정, 로그아웃 |
| `RobotDetailView.vue` | 활성화/비활성화, 닉네임 설정 |
| `CameraView.vue` | CCTV 카메라, 화면 모드, 양방향/전방만/후방만 |
| `RobotControlView.vue` | 로봇 제어, 수동 제어, 정지 |

#### Dashboard Components
| 파일 | 주요 수정 내용 |
|------|--------------|
| `AlarmNotification.vue` | 알림, 알림 아이콘, 방금 전/분/시간/일/주일 전 |
| `ListSidebarSection.vue` | 로봇 리스트, 선택된 로봇, `›`/`‹` 토글 버튼 |
| `MapSidebarSection.vue` | 실시간 로봇 위치 + **Vue 파싱 오류(따옴표 미완성) 수정** |
| `RobotList.vue` | 로봇 목록, 배터리, CPU 온도, 가동 시간, 복귀/비상정지/가동시작 |
| `RobotManagement.vue` | 로봇 관리, 상태 레이블(이동/충전/비상정지/복귀/순찰 중) |
| `RobotRegistration.vue` | 로봇 등록하기, 로봇명, IP 주소 |

#### Detail / Statistics / Camera Components
| 파일 | 주요 수정 내용 |
|------|--------------|
| `RobotInfo.vue` | 기본 정보, 배터리, 네트워크 상태, CPU 온도, 가동 시간, 3D 라이다 |
| `RobotNickname.vue` | 로봇 설정, 로봇명, 저장, 닫기 |
| `LidarViewer.vue` | 한글 정리 + SSE → 정적 PCD fallback 로직 개선 |
| `StatsChart.vue` | 가동 시간, 배터리 차트 레이블 |
| `StatsMetricBox.vue` | `{{ count }}대` 수정 |
| `StatsTable.vue` | 선택, 로봇 ID, 가동 시간, 프론트캠, 리어캠, 다운로드 |
| `Cctv.vue` | 전방/후방 카메라, 로딩 중, 다시 시도 + robotSeq prop 기본값 추가 |

---

## 3. 백엔드 로그인 버그

### 문제 1: MongoDB 비밀번호 해시 불일치

**현상**: `admin` / `admin1234` 로그인 시 401 Unauthorized

**원인**: DB에 저장된 해시가 `admin1234`와 불일치 (이전 세션에서 다른 환경으로 생성된 해시)

**해결**: Python 스크립트로 신규 bcrypt 해시 생성 후 MongoDB 업데이트
```python
pwd_context.hash("admin1234")  # → $2b$12$qowQmHuZoIuHJZyUWuvS0...
await db["users"].update_one({"username": "admin"}, {"$set": {"hashedPassword": new_hash}})
```

### 문제 2: `hashedPassword` 필드 매핑 버그

**현상**: 해시 업데이트 후에도 계속 401

**원인**: `auth_repository.py`에서 DB 필드명을 불필요하게 변환
```python
# 버그: hashedPassword → hashed_password 로 변환 (User 모델에 없는 필드명)
user_data["hashed_password"] = user_data.pop("hashedPassword")
# 결과: user.hashedPassword = None → verify_password(password, None) → 항상 False
```

**해결**: 불필요한 필드 rename 제거 (`auth_repository.py`)

### 문제 3: Token 모델 camelCase 불일치

**현상**: 비밀번호 통과 후 Pydantic 검증 오류
```
2 validation errors for Token
accessToken: Field required
refreshToken: Field required
```

**원인**: `create_tokens()`에서 snake_case로 넘기고 Token 모델은 camelCase
```python
# 버그
Token(access_token=..., refresh_token=...)
# 수정
Token(accessToken=..., refreshToken=...)
```

---

## 4. 테스트 데이터 시드

MongoDB `robots` 컬렉션이 비어있어 프론트에서 로봇 목록 미표시.

시드 스크립트 실행으로 테스트 로봇 1대 등록:
- **seq**: 1, **이름**: ROBOCOP-001 / 로보캅 1호
- **배터리**: 85%, **상태**: waiting

---

## 5. 환경 정보

- OS: Windows 11 (Win32 10.0.26200)
- Backend: Python 3.10 + FastAPI + Uvicorn + Motor (MongoDB)
- Frontend: Vue 3 + Vite
- ROS2 Bridge: WSL2 환경에서 실행
- DB: MongoDB (localhost:27017)
